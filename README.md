# CIS_Creative_Intelligence_System_v1
CIS is a system that turns **raw creative material** (files, videos, ideas) into **structured, reusable knowledge**, and then uses that knowledge to **produce real outputs** (design, animation, sound, etc.). It works by combining:  * machine interpretation (AI) * human correction * structured storage * repeatable execution 
